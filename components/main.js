import React from "react";
import Claims from "../containers/claims"

let Main = React.createClass({
    render: function () {
        return (
            <div>
                <Claims />
            </div>
        );
    }
});

module.exports = Main;